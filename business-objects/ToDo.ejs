TABLE <%= schema %>.LISTS {
    id INTEGER
    list VARCHAR(256)

    Indexes {
        (id) [pk, name:"pk"]   
    }
}
      
TABLE <%= schema %>.TASKS {
    id INTEGER
    list_id INTEGER
    task VARCHAR(16)
    complete BOOLEAN
  
    Indexes {
        (id) [pk, name:"pk"]   
    }
}